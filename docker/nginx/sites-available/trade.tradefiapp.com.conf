http {

    server {
        listen 443 ssl;
        http2 on;
        server_name trade.tradefiapp.com;

        resolver 127.0.0.11 valid=300s;

        # SSL
        ssl_certificate /etc/nginx/conf.d/domain.crt;
        ssl_certificate_key /etc/nginx/conf.d/domain.key;

        # Recommendations from https://raymii.org/s/tutorials/Strong_SSL_Security_On_nginx.html
        ssl_protocols       TLSv1.2 TLSv1.3;
        ssl_ciphers 'EECDH+AESGCM:EDH+AESGCM:AES256+EECDH:AES256+EDH';
        ssl_prefer_server_ciphers on;
        ssl_session_cache shared:SSL:10m;

        location / {
            include proxy_params;
            set $upstream http://tradefi-app:3000;
            proxy_pass $upstream;
            error_page 502 503 = @fallback;
        }
        error_page 503 @fallback;
        error_page 502 @fallback;

        location @fallback {
            root /usr/share/nginx/html;
            internal;
            try_files /fallback.html /index.html =404;
        }
    }

    server {
        listen 10000 ssl;
        http2 on;
        server_name trade.tradefiapp.com;

        resolver 127.0.0.11 valid=300s;

        # SSL
        ssl_certificate /etc/nginx/conf.d/domain.crt;
        ssl_certificate_key /etc/nginx/conf.d/domain.key;

        # Recommendations from https://raymii.org/s/tutorials/Strong_SSL_Security_On_nginx.html
        ssl_protocols       TLSv1.2 TLSv1.3;
        ssl_ciphers 'EECDH+AESGCM:EDH+AESGCM:AES256+EECDH:AES256+EDH';
        ssl_prefer_server_ciphers on;
        ssl_session_cache shared:SSL:10m;

        auth_basic           "tradefiapp";
        auth_basic_user_file /etc/nginx/conf.d/htpasswd;              # tradefiapp_user/password

        location / {
            include proxy_params;
            set $upstream http://marketbroker:8080;
            proxy_pass $upstream;
            error_page 502 503 = @fallback;
        }

        location /ws/ {
            include proxy_params_websocket;
            set $upstream http://marketbroker:8080;
            proxy_pass $upstream;
        }

        location /topic/ {
            include proxy_params_websocket;
            set $upstream http://marketbroker:8080;
            proxy_pass $upstream;
        }

        error_page 503 @fallback;
        error_page 502 @fallback;

        location @fallback {
            root /usr/share/nginx/html;
            internal;
            try_files /fallback.html /index.html =404;
        }
    }

    server {
        listen 10001 ssl;
        http2 on;
        server_name trade.tradefiapp.com;

        resolver 127.0.0.11 valid=300s;

        # SSL
        ssl_certificate /etc/nginx/conf.d/domain.crt;
        ssl_certificate_key /etc/nginx/conf.d/domain.key;

        # Recommendations from https://raymii.org/s/tutorials/Strong_SSL_Security_On_nginx.html
        ssl_protocols       TLSv1.2 TLSv1.3;
        ssl_ciphers 'EECDH+AESGCM:EDH+AESGCM:AES256+EECDH:AES256+EDH';
        ssl_prefer_server_ciphers on;
        ssl_session_cache shared:SSL:10m;

        auth_basic           "tradefiapp";
        auth_basic_user_file /etc/nginx/conf.d/htpasswd;              # tradefiapp_user/password

        location / {
            include proxy_params;
            set $upstream http://marketbroker:8090;
            proxy_pass $upstream;
            error_page 502 503 = @fallback;
        }

        error_page 503 @fallback;
        error_page 502 @fallback;

        location @fallback {
            root /usr/share/nginx/html;
            internal;
            try_files /fallback.html /index.html =404;
        }
    }


    server {
        listen 10002 ssl;
        http2 on;
        server_name trade.tradefiapp.com;

        resolver 127.0.0.11 valid=300s;

        # SSL
        ssl_certificate /etc/nginx/conf.d/domain.crt;
        ssl_certificate_key /etc/nginx/conf.d/domain.key;

        # Recommendations from https://raymii.org/s/tutorials/Strong_SSL_Security_On_nginx.html
        ssl_protocols       TLSv1.2 TLSv1.3;
        ssl_ciphers 'EECDH+AESGCM:EDH+AESGCM:AES256+EECDH:AES256+EDH';
        ssl_prefer_server_ciphers on;
        ssl_session_cache shared:SSL:10m;

        auth_basic           "tradefiapp";
        auth_basic_user_file /etc/nginx/conf.d/htpasswd;              # tradefiapp_user/password

        location / {
            include proxy_params_websocket;
            set $upstream http://grafana:3000;
            proxy_pass $upstream;
            error_page 502 503 = @fallback;
        }
        error_page 503 @fallback;
        error_page 502 @fallback;

        location @fallback {
            root /usr/share/nginx/html;
            internal;
            try_files /fallback.html /index.html =404;
        }
    }

    server {
        listen 10003 ssl;
        http2 on;
        server_name trade.tradefiapp.com;

        resolver 127.0.0.11 valid=300s;

        # SSL
        ssl_certificate /etc/nginx/conf.d/domain.crt;
        ssl_certificate_key /etc/nginx/conf.d/domain.key;

        # Recommendations from https://raymii.org/s/tutorials/Strong_SSL_Security_On_nginx.html
        ssl_protocols       TLSv1.2 TLSv1.3;
        ssl_ciphers 'EECDH+AESGCM:EDH+AESGCM:AES256+EECDH:AES256+EDH';
        ssl_prefer_server_ciphers on;
        ssl_session_cache shared:SSL:10m;

        auth_basic           "tradefiapp";
        auth_basic_user_file /etc/nginx/conf.d/htpasswd;              # tradefiapp_user/password

        location / {
            include proxy_params;
            set $upstream http://kafka-console:8080;
            proxy_pass $upstream;
            error_page 502 503 = @fallback;
        }
        error_page 503 @fallback;
        error_page 502 @fallback;

        location @fallback {
            root /usr/share/nginx/html;
            internal;
            try_files /fallback.html /index.html =404;
        }
    }

    server {
        listen 10004 ssl;
        http2 on;
        server_name trade.tradefiapp.com;

        resolver 127.0.0.11 valid=300s;

        # SSL
        ssl_certificate /etc/nginx/conf.d/domain.crt;
        ssl_certificate_key /etc/nginx/conf.d/domain.key;

        # Recommendations from https://raymii.org/s/tutorials/Strong_SSL_Security_On_nginx.html
        ssl_protocols       TLSv1.2 TLSv1.3;
        ssl_ciphers 'EECDH+AESGCM:EDH+AESGCM:AES256+EECDH:AES256+EDH';
        ssl_prefer_server_ciphers on;
        ssl_session_cache shared:SSL:10m;

        auth_basic           "tradefiapp";
        auth_basic_user_file /etc/nginx/conf.d/htpasswd;              # tradefiapp_user/password

        location / {
            include proxy_params;
            set $upstream http://questdb:9000;
            proxy_pass $upstream;
            error_page 502 503 = @fallback;
        }

        error_page 503 @fallback;
        error_page 502 @fallback;

        location @fallback {
            root /usr/share/nginx/html;
            internal;
            try_files /fallback.html /index.html =404;
        }
    }

    server {
        listen 10005 ssl;
        http2 on;
        server_name trade.tradefiapp.com;

        resolver 127.0.0.11 valid=300s;

        # SSL
        ssl_certificate /etc/nginx/conf.d/domain.crt;
        ssl_certificate_key /etc/nginx/conf.d/domain.key;

        # Recommendations from https://raymii.org/s/tutorials/Strong_SSL_Security_On_nginx.html
        ssl_protocols       TLSv1.2 TLSv1.3;
        ssl_ciphers 'EECDH+AESGCM:EDH+AESGCM:AES256+EECDH:AES256+EDH';
        ssl_prefer_server_ciphers on;
        ssl_session_cache shared:SSL:10m;

        auth_basic           "tradefiapp";
        auth_basic_user_file /etc/nginx/conf.d/htpasswd;              # tradefiapp_user/password

        location / {
            include proxy_params;
            set $upstream http://prometheus:9090;
            proxy_pass $upstream;
#            proxy_pass http://prometheus:9090;
            error_page 502 503 = @fallback;
        }
        error_page 503 @fallback;
        error_page 502 @fallback;

        location @fallback {
            root /usr/share/nginx/html;
            internal;
            try_files /fallback.html /index.html =404;
        }
    }

    server {
        listen 10006 ssl;
        http2 on;
        server_name trade.tradefiapp.com;

        resolver 127.0.0.11 valid=300s;

        # SSL
        ssl_certificate /etc/nginx/conf.d/domain.crt;
        ssl_certificate_key /etc/nginx/conf.d/domain.key;

        # Recommendations from https://raymii.org/s/tutorials/Strong_SSL_Security_On_nginx.html
        ssl_protocols       TLSv1.2 TLSv1.3;
        ssl_ciphers 'EECDH+AESGCM:EDH+AESGCM:AES256+EECDH:AES256+EDH';
        ssl_prefer_server_ciphers on;
        ssl_session_cache shared:SSL:10m;

        auth_basic           "tradefiapp";
        auth_basic_user_file /etc/nginx/conf.d/htpasswd;              # tradefiapp_user/password

        location / {
            include proxy_params;
            set $upstream http://phpmyadmin:80;
            proxy_pass $upstream;
            error_page 502 503 = @fallback;
        }

        error_page 503 @fallback;
        error_page 502 @fallback;

        location @fallback {
            root /usr/share/nginx/html;
            internal;
            try_files /fallback.html /index.html =404;
        }
    }

    server {
        listen 10007 ssl;
        http2 on;
        server_name trade.tradefiapp.com;

        resolver 127.0.0.11 valid=300s;

        # SSL
        ssl_certificate /etc/nginx/conf.d/domain.crt;
        ssl_certificate_key /etc/nginx/conf.d/domain.key;

        # Recommendations from https://raymii.org/s/tutorials/Strong_SSL_Security_On_nginx.html
        ssl_protocols       TLSv1.2 TLSv1.3;
        ssl_ciphers 'EECDH+AESGCM:EDH+AESGCM:AES256+EECDH:AES256+EDH';
        ssl_prefer_server_ciphers on;
        ssl_session_cache shared:SSL:10m;

#        auth_basic           "tradefiapp";
#        auth_basic_user_file /etc/nginx/conf.d/htpasswd;              # tradefiapp_user/password

        location / {
            include proxy_params;
            set $upstream http://mongo-express:8081;
            proxy_pass $upstream;
            error_page 502 503 = @fallback;
        }

        error_page 503 @fallback;
        error_page 502 @fallback;

        location @fallback {
            root /usr/share/nginx/html;
            internal;
            try_files /fallback.html /index.html =404;
        }
    }
}
